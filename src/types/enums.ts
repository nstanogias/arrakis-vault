export enum Network {
  Arbitrum = "Arbitrum",
  Ethereum = "Ethereum",
  Polygon = "Polygon",
}

export enum TokenSymbol {
  WETH = "WETH",
  rETH = "rETH",
  USDC = "USDC",
}

export enum ContractCallResponseStatus {
  Success = "success",
  Failure = "failure",
}
